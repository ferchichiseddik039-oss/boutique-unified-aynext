{
  "name": "boutique-unified",
  "version": "1.0.0",
  "description": "Application unifiée - Frontend React + Backend Express",
  "main": "backend/server.js",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "bash build-final.sh",
    "build:yarn": "cd frontend && rm -rf node_modules yarn.lock && yarn install --ignore-engines && chmod +x node_modules/.bin/* && yarn build",
    "build:simple": "cd frontend && npm install && npx react-scripts build",
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "engines": {
    "node": "16.x"
  },
  "keywords": [
    "e-commerce",
    "react",
    "express",
    "mongodb",
    "boutique",
    "vetements"
  ],
  "author": "AYNEXT",
  "license": "MIT"
}
