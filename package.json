{
  "name": "boutique-unified",
  "version": "1.0.0",
  "description": "Application unifiée - Frontend React + Backend Express",
  "main": "backend/server.js",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && rm -rf node_modules package-lock.json && npm install && chmod +x node_modules/.bin/* && npm run build",
    "build:simple": "cd frontend && npm install && npx react-scripts build",
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "engines": {
    "node": "20.x"
  },
  "keywords": [
    "e-commerce",
    "react",
    "express",
    "mongodb",
    "boutique",
    "vetements"
  ],
  "author": "AYNEXT",
  "license": "MIT"
}
